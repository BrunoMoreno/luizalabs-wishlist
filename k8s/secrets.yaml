apiVersion: v1
kind: Secret
metadata:
  name: wishlist-secrets
type: Opaque
stringData:
  database-url: postgresql://postgres:postgres@postgres:5432/wishlist
  secret-key: your-production-secret-key
  postgres-user: postgres
  postgres-password: postgres 